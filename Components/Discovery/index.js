import React,{memo} from 'react'
import { View, Text } from 'react-native'

const Discovery = () => {
    return (
        <View>
            <Text>Discovery</Text>
        </View>
    )
}

export default memo(Discovery)
